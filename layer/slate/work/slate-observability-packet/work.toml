id = "slate-observability-packet"
title = "Add Slate observability packet and metrics"
kind = "build"
status = "complete"
human_request = "Make standalone Slate complete and observable enough that an agent can answer exactly what is going on, why, what changed, what proof remains, and where cleanup is needed."
user_value = "Agents and humans can ask Slate what is happening and get an evidence-backed answer instead of relying on conversation memory."
scope = [
    "Enrich Slate work packets with deterministic state, proof, blocker, cleanup, evidence, and next-action data.",
    "Emit host-visible Slate operation, lifecycle, check, and packet metrics through patina:measure.",
    "Tighten ready gates so work cannot become ready without user value, scope, non-goals, stop condition, and proof.",
]
non_goals = [
    "Do not redesign Patina Mother.",
    "Do not replace the existing spec bridge in this slice.",
    "Do not implement Grafana dashboards inside the Slate child repo.",
]
stop_condition = "Stop when packet-work exposes the state contract, metrics are emitted, tests pass, and the Grafana-Labs observer can still consume Slate state."
allium_anchors = [
    "layer/core/spec-driven-design.md",
    "layer/core/session-capture.md",
    "layer/core/safety-boundaries.md",
]
user_alignment = "This keeps Slate close to product closure rather than theory by adding deterministic packet/state surfaces, lifecycle/proof evidence, and metrics for Grafana/agent observability."
belief_refs = []
proof_plan = [
    "[x] cargo test passes in patina-child-slate after packet/metrics changes.",
    "[x] `patina child call slate-manager patina:slate/control@0.1.0.packet-work` returns status, proof progress, blockers, evidence, and next action for this work item.",
    "[x] Slate emits operation/check/packet metrics through the existing patina:measure import or an equivalent host-visible path.",
    "[x] Grafana-Labs Rust slate-observer can consume the updated packet shape without losing current /state and /metrics behavior.",
    "[x] Documentation explains the agent evidence protocol and the boundaries between Slate child, Patina host CLI, and Grafana observer.",
]
closure_evidence = [
    "cargo test passed in /Users/nicabar/Projects/Patina/patina-child-slate after packet/metrics changes.",
    "cargo component build --release produced target/wasm32-wasip1/release/patina_ai_child_slate_manager.wasm.",
    "Telemetry helpers record check, packet, operation, and lifecycle transition metrics; native tests enforce metric-name/value invariants without WASI imports.",
    "Grafana-Labs Rust slate-observer consumed the updated work store and reported slate-observability-packet as active with low drift risk.",
    "README.md and skills/slate-code/SKILL.md document the agent packet protocol and product-closure fields.",
    "Installed rebuilt slate-manager locally after backup and packet-work smoke returned state.user-value, scope, non-goals, stop-condition, progress, next-safe-action, cleanup-candidates, and evidence.",
]
blocked_by = []
blocks = []
target = "standalone-slate-observability"
implementation_plan = [
    "Add or enrich packet-work so agents can fetch deterministic state for a work item without guessing.",
    "Add observability metrics for Slate operations, lifecycle transitions, proof gates, and packet requests using patina:measure.",
    "Expose missing top-level Patina CLI parity for read-only Slate operations first: list, ready, blocked, history, prompt, handoff, packet.",
    "Keep the outside-in Rust slate-observer in Grafana-Labs as the proving harness for state shape, metrics, and Grafana dashboards.",
    "Document the agent protocol: answer “what is going on?” only after reading Slate packet/history/measure evidence.",
]
belief_harvest_decision = "no new belief; this work reinforces existing spec-driven design and session-capture doctrine."
created_at = "1782738237"
updated_at = "1782739617"
closed_at = "1782739617"
